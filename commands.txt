first time when coonect to repo
-------------------------
git init
git status
git add *
git commit -m "message"
git status
git remote add origin repoUrl
git remote -v
git push origin master


day to day for a repo ( changes done then )
-------------------------


git add *
git status
git commit -m "message"
git push origin master
